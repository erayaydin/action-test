entrypoint: api/server-api.yaml
mode: multi_file
file_mappings:
  - source: api.yaml
    target: out.yaml
internal:
  file_marker: x-internal
  strip_fields:
    - x-internal
  exclude_patterns: "not an array"
