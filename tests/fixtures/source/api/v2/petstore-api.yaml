openapi: '3.1.0'
info:
  title: Petstore API
  version: '2.0'
paths:
  /pets:
    $ref: './paths/pets.yaml'
  /pets/{id}:
    $ref: './paths/pet.yaml'
  /internal-metrics:
    $ref: './paths/internal-metrics.yaml'
components:
  schemas:
    Pet:
      $ref: './components/schemas/Pet.yaml'
    InternalDiagnostics:
      $ref: './components/schemas/InternalDiagnostics.yaml'
    MixedModel:
      $ref: './components/schemas/MixedModel.yaml'
